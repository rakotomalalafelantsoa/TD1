#include <stdio.h>
int main ()
{
  int x,y;
  printf ("Veuillez saisir un nombre:");
  scanf("%d",&x);
  if ((x>0 && y>0)|| (x<0 && y<0))
  {
    printf ("Le produit est positif:\n");
  }
    else  
    {
      printf ("Le produit est positif:\n");
    }
  return (0);
}
      
